[build-system]

requires = [
    "setuptools",
    "setuptools_scm",
    "wheel",
    "extension-helpers",
    "oldest-supported-numpy",
]

build-backend = 'setuptools.build_meta'

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
    "zdm/tests",
]